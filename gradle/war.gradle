apply plugin: "war"

bootWar {
    mainClassName = "com.code.challenge.itinerary.ItineraryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
